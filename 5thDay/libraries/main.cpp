#include <iostream>
#include <iomanip> // std::setprecision
#include <vector>

#include "header.h"

int main(){
    int n,m;
    n = 5; m = 5;
    
    print( add( n, m ) );
    print( mult( n, m ) );

    
}
